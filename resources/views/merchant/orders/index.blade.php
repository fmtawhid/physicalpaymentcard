@extends('layouts.master')

@section('content')
<main class="min-h-screen bg-slate-50 py-12"><div class="mx-auto max-w-6xl px-4"><div class="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p class="text-sm font-black uppercase tracking-wider text-emerald-600">আমার অর্ডার</p><h1 class="mt-2 text-3xl font-black text-slate-950">Order History</h1><p class="mt-2 text-slate-500">আপনার card order-এর status এবং card information দেখুন।</p></div><a href="{{ url('/') }}#order" class="rounded-xl bg-slate-950 px-5 py-3 text-sm font-bold text-white">নতুন কার্ড কিনুন</a></div>@if(session('success'))<div class="mt-6 rounded-2xl bg-emerald-50 p-4 text-emerald-800">{{ session('success') }}</div>@endif<div class="mt-7 overflow-x-auto rounded-3xl bg-white shadow-xl"><table class="min-w-full divide-y divide-slate-100"><thead class="bg-slate-50"><tr><th class="px-6 py-4 text-left text-xs font-black uppercase text-slate-500">#</th><th class="px-6 py-4 text-left text-xs font-black uppercase text-slate-500">Product</th><th class="px-6 py-4 text-left text-xs font-black uppercase text-slate-500">Amount</th><th class="px-6 py-4 text-left text-xs font-black uppercase text-slate-500">Status</th><th class="px-6 py-4 text-left text-xs font-black uppercase text-slate-500">Action</th></tr></thead><tbody class="divide-y divide-slate-100">@forelse($orders as $order)<tr><td class="px-6 py-5 text-sm text-slate-500">#{{ $order->id }}</td><td class="px-6 py-5 font-bold text-slate-800">{{ $order->product?->name ?? 'Product removed' }}<div class="mt-1 text-xs font-normal text-slate-400">{{ $order->created_at->format('d M Y, h:i A') }}</div></td><td class="px-6 py-5 text-sm text-slate-700">{{ $order->product?->currency ?? 'USD' }} {{ number_format((float) $order->amount, 2) }}</td><td class="px-6 py-5"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $order->status === 'approved' ? 'bg-emerald-100 text-emerald-700' : ($order->status === 'rejected' ? 'bg-red-100 text-red-700' : 'bg-amber-100 text-amber-700') }}">{{ ucfirst($order->status) }}</span></td><td class="px-6 py-5"><a href="{{ route('orders.show', $order) }}" class="font-bold text-emerald-700 hover:text-emerald-900">বিস্তারিত →</a></td></tr>@empty<tr><td colspan="5" class="px-6 py-14 text-center text-slate-500">আপনার কোনো order নেই।</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $orders->links() }}</div></div></main>
@endsection
