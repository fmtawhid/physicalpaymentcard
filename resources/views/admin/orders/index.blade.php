@extends('admin.layout.layout')

@section('content')
<main class="flex-1 overflow-y-auto bg-gray-50 p-4 md:p-6"><div class="mx-auto max-w-7xl"><div class="mb-6 flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><h1 class="text-2xl font-bold text-gray-800">Card Orders</h1><p class="mt-1 text-sm text-gray-500">Review payment slips, update status and provide card information.</p></div><div class="rounded-lg bg-white px-4 py-3 text-sm text-gray-600 shadow">Total orders: <strong class="text-gray-900">{{ $orders->total() }}</strong></div></div>@if(session('success'))<div class="mb-5 rounded-lg bg-green-100 px-4 py-3 text-green-800">{{ session('success') }}</div>@endif<div class="overflow-x-auto rounded-xl bg-white shadow-lg"><table class="min-w-full divide-y divide-gray-200"><thead class="bg-gray-100"><tr><th class="px-5 py-3 text-left text-xs font-bold uppercase text-gray-600">Order</th><th class="px-5 py-3 text-left text-xs font-bold uppercase text-gray-600">Customer</th><th class="px-5 py-3 text-left text-xs font-bold uppercase text-gray-600">Product</th><th class="px-5 py-3 text-left text-xs font-bold uppercase text-gray-600">Payment</th><th class="px-5 py-3 text-left text-xs font-bold uppercase text-gray-600">Status</th><th class="px-5 py-3 text-left text-xs font-bold uppercase text-gray-600">Action</th></tr></thead><tbody class="divide-y divide-gray-200">@forelse($orders as $order)<tr class="hover:bg-gray-50"><td class="px-5 py-4 text-sm"><strong>#{{ $order->id }}</strong><div class="mt-1 text-xs text-gray-400">{{ $order->created_at->format('d M Y, h:i A') }}</div></td><td class="px-5 py-4 text-sm"><strong>{{ $order->customer_name }}</strong><div class="text-xs text-gray-500">{{ $order->customer_phone }}</div><div class="text-xs text-gray-500">{{ $order->customer_email }}</div></td><td class="px-5 py-4 text-sm text-gray-700">{{ $order->product?->name ?? 'Product removed' }}<div class="text-xs text-gray-500">{{ $order->product?->currency ?? 'USD' }} {{ number_format((float) $order->amount, 2) }}</div></td><td class="px-5 py-4 text-sm text-gray-700">{{ $order->payment_method }}<div class="text-xs text-gray-500">{{ $order->payment_number }}</div>@if($order->payment_slip)<a class="mt-1 inline-block text-xs font-bold text-blue-600 hover:underline" target="_blank" href="{{ asset('uploads/order-slips/'.$order->payment_slip) }}">View slip</a>@endif</td><td class="px-5 py-4"><span class="rounded-full px-3 py-1 text-xs font-bold {{ $order->status === 'approved' ? 'bg-green-100 text-green-800' : ($order->status === 'rejected' ? 'bg-red-100 text-red-800' : 'bg-amber-100 text-amber-800') }}">{{ ucfirst($order->status) }}</span></td><td class="px-5 py-4"><div class="flex gap-2"><a href="{{ route('admin.order.edit', $order) }}" class="rounded bg-blue-500 px-3 py-1 text-sm text-white hover:bg-blue-600">Review</a><form method="POST" action="{{ route('admin.order.destroy', $order) }}">@csrf @method('DELETE')<button onclick="return confirm('Delete this order?')" class="rounded bg-red-500 px-3 py-1 text-sm text-white hover:bg-red-600">Delete</button></form></div></td></tr>@empty<tr><td colspan="6" class="px-5 py-12 text-center text-gray-500">No orders found.</td></tr>@endforelse</tbody></table></div><div class="mt-6">{{ $orders->links() }}</div></div></main>
@endsection
